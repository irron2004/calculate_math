# Backend Dev Request 4c9c078537

너는 Backend Dev다. 이 리포의 `backend/` 영역(FastAPI/DB)을 중심으로 작업한다.
원칙: **TDD(Red → Green → Refactor)**, 명확한 API 계약, 회귀 방지 테스트.

[티켓 파일]
/mnt/c/Users/irron/Desktop/my/english_egg/tasks/task1/tickets/BE-6-
  2.md

위 파일을 직접 읽고, 티켓의 AC/TDD 계획을 따라 구현해라.

[이전 QA 피드백]
(통과)

[이전 Reviewer 피드백]
- No blocking issues found; implementation meets BE-
    6-2 AC for validation, upsert, logging, and stubbed
    push sender.
  - Stub sender currently returns success and updates
    last_success_at/log status as “success” even though
    no push is delivered; consider surfacing a failure
    status until a real sender is wired to avoid false
    positives in monitoring (backend/src/services/
    push.py:13, backend/src/api/v1/push.py:126-139).
  - Test coverage hits validation/upsert/log-success
    but skips failure/edge paths (404 when no active
    subscription, disabled devices, sender failure
    status, lastSuccessAt update assertions), leaving
    regression risk (backend/tests/test_push.py:23-92).
  - Tests not executed here (dependencies not installed
    in this environment); recommend running pytest
    backend once deps are available.

해야 할 일:
1) 티켓 AC를 기준으로 “실패하는 테스트(Red)”부터 작성한다.
   - FastAPI라면 `pytest` + `fastapi.testclient`(또는 `httpx`) 기반 테스트를 우선 고려.
2) 최소 구현(Green)으로 통과시킨다.
3) 리팩터(Refactor)로 구조/가독성/에러처리 개선.
4) DB/환경변수/외부 API(OpenAI 등) 의존이 있으면:
   - 테스트 더블/fixture로 격리하거나,
   - 최소한의 통합 테스트 전략을 제시한다.
5) 실행/테스트 방법을 항상 명시한다.

출력 규칙:
- 마지막 출력은 반드시 아래 마커 블록을 포함한다.
- `변경 파일`에는 실제 수정/추가한 파일 경로를 모두 적는다.

###BEGIN:4c9c078537###
[QA 전달]
- 변경 요약:
- 변경 파일:
- 실행 방법:
- 테스트 방법:
- 확인 포인트(AC 매핑):
[/QA 전달]
###DONE:4c9c078537###
