# QA Request 9864b32a73

아래 티켓과 Dev 전달 내용을 기준으로 검증해줘.

[티켓 파일]
/mnt/c/Users/irron/Desktop/my/english_egg/tasks/task1/tickets/BE-1.md

[Dev 전달 내용]
[QA 전달]

  - 변경 요약:
      - backend/src/api/v1/students.py에 남아있던 datetime.utcnow() 호출을
        제거(DeprecationWarning 재발 방지)하고, 기존 응답 포맷(naive ISO
        string)은 유지하도록 UTC now를 naive로 변환해 사용.
      - 해당 경고가 실제로 다시 생기지 않도록 /api/v1/missions/
        {mission_id}/complete 호출 경로에 대해 DeprecationWarning을 error
        로 승격하는 회귀 방지 테스트 추가(TDD: Red→Green).
  - 변경 파일:
      - backend/src/api/v1/students.py
      - backend/tests/test_datetime_deprecations.py
  - 실행 방법:
      - cd backend
      - python -m venv .venv && .venv/bin/pip install -r requirements.txt
      - export DATABASE_URL='postgresql://
        postgres:postgres@localhost:5432/english_egg'
      - .venv/bin/uvicorn backend.src.main:app --reload --port 4000
  - 테스트 방법:
      - cd backend && .venv/bin/python -m pytest -q
  - 확인 포인트(AC 매핑):
      - AC1 /health 200 + version/db_status: backend/src/main.py:1,
        backend/tests/test_health.py:1
      - AC2 DATABASE_URL fail-fast + 로딩/정규화: backend/src/config.py:1,
        backend/tests/test_config.py:1
      - AC3 마이그레이션 create/apply/rollback + staging URL: backend/src/
        cli.py:1, backend/tests/test_migrations_cli.py:1
      - AC4 cd backend 기준 import/hot reload 커맨드: backend/backend/
        __init__.py:1, backend/tests/test_package_layout.py:1
      - (회귀 방지) datetime.utcnow() deprecation 재발 방지: backend/src/
        api/v1/students.py:1, backend/tests/
        test_datetime_deprecations.py:1
        [/QA 전달]

규칙(엄수):
- 첫 줄은 반드시 OK 또는 FAIL 로 시작해라.
- 가능한 경우 Dev가 제시한 테스트 커맨드를 실제로 실행하고, 결과(성공/실패/로그 요약)를 리포트에 포함해라.
- Dev가 테스트 방법을 비워뒀거나, 변경이 있는데 테스트가 없다면 그 자체를 FAIL 사유로 삼아도 된다(특히 버그 수정/회귀 위험이 큰 변경).
- FAIL이면 반드시 포함:
  - 재현 단계
  - 어떤 로그/증상이 문제인지
  - 원인 추정
  - 수정 제안(Dev가 바로 고칠 수 있게)
- 마지막에 아래 마커를 정확히 출력해라.

###BEGIN:9864b32a73###
(여기에 네 QA 리포트 본문을 작성)
###DONE:9864b32a73###
