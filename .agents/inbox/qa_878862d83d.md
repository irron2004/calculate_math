# QA Request 878862d83d

아래 티켓과 Dev 전달 내용을 기준으로 검증해줘.

[티켓 파일]
/mnt/c/Users/irron/Desktop/my/english_egg/tasks/task1/tickets/BE-1.md

[Dev 전달 내용]
[QA 전달]

  - 변경 요약:
      - DATABASE_URL 필수/유효성 검증 강화 + postgresql:///sqlite://를
        async 드라이버 URL로 자동 정규화
      - cd backend 기준으로 backend.src.* import가 동작하도록 패키지 shim
        추가 (티켓 run/test 커맨드 실동작 보장)
      - 마이그레이션 CLI의 staging URL 선택(--env staging)을 테스트로 고정
      - startup 자동 테이블 생성 제거(health endpoint가 DB 다운 상황에서도
        기동 가능, 마이그레이션 기반 운영 유도)
      - .env.example에 DB 환경변수 예시 추가
  - 변경 파일:
      - backend/src/config.py
      - backend/src/main.py
      - backend/.env.example
      - backend/backend/init.py
      - backend/tests/test_package_layout.py
      - backend/tests/test_config.py
      - backend/tests/test_migrations_cli.py
  - 실행 방법:
      - cd backend
      - python -m venv .venv && .venv/bin/pip install -r requirements.txt
      - export DATABASE_URL='postgresql://
        postgres:postgres@localhost:5432/english_egg'
      -  .venv/bin/uvicorn backend.src.main:app --reload --port 4000
  - 테스트 방법:
      - cd backend && .venv/bin/python -m pytest -q
  - 확인 포인트(AC 매핑):
      - GET /health가 200 + {"version": ..., "db_status": ...} 반환:
        backend/src/main.py:1, backend/tests/test_health.py:1
      - DATABASE_URL env 로드 + 누락/비정상 즉시 실패: backend/src/
        config.py:1, backend/tests/test_config.py:1
      - 마이그레이션 create/apply/rollback + staging config 지원: backend/
        src/cli.py:1, backend/tests/test_migrations_cli.py:1
      - cd backend && uvicorn backend.src.main:app --reload 실행 가능:
        backend/backend/__init__.py:1, backend/tests/
        test_package_layout.py:1
        [/QA 전달]

규칙(엄수):
- 첫 줄은 반드시 OK 또는 FAIL 로 시작해라.
- 가능한 경우 Dev가 제시한 테스트 커맨드를 실제로 실행하고, 결과(성공/실패/로그 요약)를 리포트에 포함해라.
- Dev가 테스트 방법을 비워뒀거나, 변경이 있는데 테스트가 없다면 그 자체를 FAIL 사유로 삼아도 된다(특히 버그 수정/회귀 위험이 큰 변경).
- FAIL이면 반드시 포함:
  - 재현 단계
  - 어떤 로그/증상이 문제인지
  - 원인 추정
  - 수정 제안(Dev가 바로 고칠 수 있게)
- 마지막에 아래 마커를 정확히 출력해라.

###BEGIN:878862d83d###
(여기에 네 QA 리포트 본문을 작성)
###DONE:878862d83d###
