# Learnings

- Added `node_guides_2022_v1.json` with minimal versioned schema and explicit fallback strings for UI rendering.
- Integrated hover panel with node guide lookup and fallback sections: `요약 목표`, `문제 생성 가이드`.
- Added targeted tests for seeded guide rendering and `(준비중)` fallback behavior.
- Added authoring doc at `curriculum-viewer/docs/node-guide-authoring.md` linked to problem-generation workflow.
