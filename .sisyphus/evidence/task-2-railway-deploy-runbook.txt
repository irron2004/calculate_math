Task 2 Evidence: Railway Deploy Mitigation (exclude-patterns header issue)
Source plan: .sisyphus/plans/.legacy/homework-log-rendering-fix.md (Task 2)
Date: 2026-03-05

Goal:
- Reproduce `railway up` failure and document mitigation or fallback deployment path.

Environment:
- Working directory: curriculum-viewer/
- Railway CLI: `railway 4.30.1`

Reproduction attempt:
1) Command: `railway --version`
   - Result: `railway 4.30.1`
2) Command: `railway up`
   - Result: SUCCESS (no `exclude-patterns` header failure reproduced)
   - Output included:
     - `Indexing...`
     - `Uploading...`
     - Build logs URL generated by Railway

Ignore/config character audit (for known symptom hardening):
Checked files:
- `curriculum-viewer/.railwayignore`
- `curriculum-viewer/.dockerignore`
- `.gitignore`

Check method:
- Python byte scan for UTF-8 BOM, non-printable control chars, non-ASCII bytes, and CRLF.

Results:
- `.railwayignore`: bom=False, bad_ctrl=0, non_ascii=0, crlf=False
- `.dockerignore`: bom=False, bad_ctrl=0, non_ascii=0, crlf=False
- `.gitignore`: bom=False, bad_ctrl=0, non_ascii=0, crlf=False

Working deploy path:
- Primary path works as-is:
  1. `cd curriculum-viewer`
  2. `railway --version`
  3. `railway up`

Fallback path (if header issue reappears later):
1) Re-run ignore file byte audit above.
2) Normalize suspect files to LF + ASCII (remove BOM/control chars).
3) Re-test `railway up`.
4) If still failing, use Railway GitHub integration deployment path from Railway dashboard for the same service.

Monorepo root-directory stabilization (2026-03-06):
- Keep service `rootDirectory=curriculum-viewer`.
- Avoid `railway up "curriculum-viewer" --path-as-root` for this service, because that can produce `Could not find root directory: curriculum-viewer`.
- Preferred deploy shape: execute `railway up` from a clean parent folder that contains `curriculum-viewer/` as a direct child.

Secrets policy:
- No tokens, credentials, or private keys recorded in this runbook.
