{"task_id":"2025-12-02-1","owner_role":"security","status":"needs-revision","artifacts_out":[{"type":"doc","uri":"docs/THREAT_MODEL.md","note":"Claims Argon2/HTTPS-only controls are live (lines 11-22) even though the router is non-executable; update once fixes land."},{"type":"doc","uri":"docs/SECURITY_REPORT.md","note":"States Argon2 hashing + rate limiting shipped (lines 3-17) despite missing implementation; revise after remediation."}],"risks":["app/routers/practice.py:1 still contains the literal diff output, so the module will not import and none of the Argon2 hashing, HTTPS-only cookie, or rate limiting logic actually executes—login security requirements remain unmet.","tests/test_api.py:1 and tests/test_skills_router.py:1 are also diff stubs, meaning the promised regression tests for Argon2 migration, HTTPS cookie enforcement, and fallback telemetry cannot even run, leaving us without evidence for requirement #2.","docs/SECURITY_REPORT.md:3-17 and docs/THREAT_MODEL.md:11-22 attest that Argon2 hashing, secure SameSite cookies, and per-IP rate limiting are enforced even though the codebase is non-functional, creating a compliance/audit exposure.","app/config.py:170 still defaults PASSWORD_PEPPER to the public string \"calculate-pepper\"; if operators forget to override it, every Argon2 hash would share a known pepper, weakening credential storage."],"action_items":[{"assignee":"backend","action":"Restore app/routers/practice.py with real FastAPI code implementing Argon2id hashing, HTTPS-only cookie issuance, and login rate limiting, then rerun pytest/make smoke to prove the controls.", "priority":"high"},{"assignee":"qa_release","action":"Once backend fixes land, capture pytest -q (or make smoke) output showing the Argon2 migration, HTTPS cookie rejection, and rate-limit behavior as required evidence for items #2-5.", "priority":"high"},{"assignee":"security","action":"After the implementation is real, update docs/SECURITY_REPORT.md and docs/THREAT_MODEL.md to match deployed controls and document PASSWORD_PEPPER as a mandatory secret override.", "priority":"medium"}],"handoff_to":["qa_release","executive"],"confidence":0.67}