{"task_id":"2025-12-02-1","owner_role":"qa_release","status":"needs-revision","decision":"request_changes","failures":[{"test":"readyz-smoke","reason":"`pytest tests/test_api.py -k readyz` cannot even import the app because app/config.py:1 literally contains git diff metadata (starts with `diff --git…`), triggering `SyntaxError: invalid syntax` and blocking /readyz verification."},{"test":"skills-tree-api","reason":"The skills router under app/routers/skills.py also begins with diff metadata instead of Python code, so FastAPI cannot load `/api/v1/skills/tree`; this violates the requirement to prove the fallback graph works in a clean environment."},{"test":"login-security","reason":"Authentication still hashes passwords with unsalted SHA-256 and writes lax cookies (`_hash_password` + `_set_session_cookie` in app/routers/practice.py:80-118), so the mandated bcrypt/argon2 upgrade, SESSION_COOKIE_SECURE default True, SameSite=strict, and rate limiting remain missing."},{"test":"frontend-vitest","reason":"Running `CI=1 npm test -- --run` in frontend/ reproduces the tinypool crash (`Error: Worker exited unexpectedly`); with SkillTreePage.tsx/api.ts files also stored as diff text, we cannot validate the auto-seed fallback UX or telemetry."],"action_items":[{"assignee":"backend","action":"Restore runnable modules (app/config.py, app/routers/skills.py) and share clean `/readyz` + `/api/v1/skills/tree` responses alongside a passing `pytest tests/test_api.py -k readyz` log.", "priority":"high"},{"assignee":"security","action":"Replace the SHA-256 login hash with bcrypt/argon2, force secure/strict cookies, add login rate limiting, and provide regression tests or logs proving the protections.", "priority":"high"},{"assignee":"frontend","action":"Resolve the Vitest tinypool crash, ensure SkillTreePage/api utilities are valid TypeScript, and document auto-seed fallback behavior with passing `CI=1 npm test -- --run` output.", "priority":"high"}],"handoff_to":["executive"],"confidence":0.34}
